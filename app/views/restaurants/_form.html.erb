<div class="banner" style="background-image: linear-gradient(-125deg, rgba(5,101,168,0.5) 0%, rgba(0,36,61,0.4) 20%), url('<%= image_path('background.jpg')%>');">
  <div class="container">
    <div class="form-row">
      <div class="col-sm-4 col-sm-offset-4">
        <div class="logins">
          <p class="logins-title">Ajouter un restaurant</p>
            <%= simple_form_for(@restaurant) do |f| %>
              <%= f.error_notification %>

              <div class="form-inputs input-claire">
                <%= f.input :name, placeholder: "Le nom du restaurant", label: false %>
                <%= f.input :chef, placeholder: "Ouh la toque", label: false %>
                <%= f.input :address, input_html: { class: "address-autocomplete" }, label: false %>
                <%= f.input :description, placeholder: "C'était comment?", label: false %>
                <%= f.input :phone, placeholder: "Tel", label: false %>
                <%= f.input :average_rating, placeholder: "0 - 5 ?", label: false %>
             <!--    <%= f.input attachinary %> -->
              </div>

              <div class="form-action">

              <%= f.submit "Mettre à jour", class: "form-button" %>
              <%= link_to "Retour", user_path(current_user), class: "back-button" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

